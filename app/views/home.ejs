<!DOCTYPE html>
<html>

<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td,
        th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;
        }
    </style>
</head>

<body>

    <h2>1 acesso = 1 novo user</h2>

    <table>
        <tr>
            <th>Id</th>
            <th>Nome</th>
        </tr>
        <% for (person of peopleList) { %>
            <tr>
                <td>
                    <%= person.id %>
                </td>
                <td>
                    <%= person.name %>
                </td>

            </tr>
        <% } %>
    </table>
</body>

</html>